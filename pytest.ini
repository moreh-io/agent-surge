[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
filterwarnings =
    error
    ignore::pytest.PytestUnraisableExceptionWarning
markers =
    real_cli: requires real codex/claude/opencode binary; runs only when AGENTSURGE_E2E_FRONTENDS=1 is set
